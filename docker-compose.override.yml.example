services:
  app:
    environment:
      - WAGTAIL_SITE_HOSTNAME=localhost:65500  # The hostname of the site we want to make requests top
      - CACHE_DEFAULT_TIMEOUT=15  # Sensible cache timeout for testing (seconds)
