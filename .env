# Flask Development Environment Configuration
# This file is for local development only and should not be committed to git

# Use Develop configuration class (enables debug mode and short cache timeout)
CONFIG=config.Develop

# Enable Flask debug mode (auto-reload templates, detailed errors)
DEBUG=True

# Optional: Set Flask app (if using 'flask run' command)
FLASK_APP=main.py
FLASK_DEBUG=1

# Optional: Custom development settings
# SECRET_KEY=your-dev-secret-key
# COOKIE_DOMAIN=localhost
# GA4_ID=
